# Pinned Unicode emoji data

This directory contains the exact Unicode Emoji 17.0 data inputs used to validate and generate CULT Emoji Registry V1. They are deliberately committed and never auto-updated. See `manifest.json` for provenance and SHA-256 checksums and `LICENSE.txt` for attribution.
