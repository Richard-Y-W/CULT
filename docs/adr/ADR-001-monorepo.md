# ADR-001: TypeScript workspace monorepo

Status: accepted. Use npm workspaces and TypeScript project references. Shared types and domain logic remain independently testable while a single install and process pair keeps V0 approachable.
