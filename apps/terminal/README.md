# Terminal

Analyst Mode is shipped as the `/terminal` route in `apps/web` so authentication, routing, and rendering stay unified in V0. This boundary remains reserved for a separately deployed terminal if bundle or product needs justify it.
