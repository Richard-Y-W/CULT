import{describe,expect,it}from'vitest';import{acceleration,breadth,canonicalId,generateSynthetic,normalizeExpression,persistence,usagePerMillion}from'@cult/expression-engine';
describe('expression engine',()=>{it('normalizes explicit phrase aliases without aggressive merging',()=>{expect(normalizeExpression('We Are Cooked')).toBe("we're cooked");expect(normalizeExpression('  LMAO  ')).toBe('lmao');expect(normalizeExpression('different phrase')).toBe('different phrase')});it('produces stable ids and document prevalence',()=>{expect(canonicalId("we're cooked")).toBe('expr_we_re_cooked');expect(usagePerMillion(10,1000)).toBe(10000)});it('calculates movement features',()=>{expect(acceleration([1,3,8])).toBe(3);expect(breadth([1,2,-1,3])).toBe(.75);expect(persistence([1,2,-1,3])).toBe(.5)});it('is deterministic and narratively structured',()=>{const a=generateSynthetic(42,30),b=generateSynthetic(42,30);expect(a.history.expr_crying_face).toEqual(b.history.expr_crying_face);expect(a.observations).toHaveLength(19*30*4);expect(a.semantics.expr_crying_face).toHaveLength(30)})});
