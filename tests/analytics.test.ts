import{describe,expect,it}from'vitest';import{beta,correlation,drawdown,logReturns,momentum,runBacktest,simpleReturns,volatility,zScore}from'@cult/analytics';
describe('analytics',()=>{it('calculates returns and momentum',()=>{simpleReturns([100,110,121]).forEach(x=>expect(x).toBeCloseTo(.1));expect(logReturns([1,Math.E])).toEqual([1]);expect(momentum([100,105,120],2)).toBeCloseTo(.2)});it('calculates risk statistics',()=>{expect(volatility([1,2,3])).toBe(1);expect(correlation([1,2,3],[2,4,6])).toBeCloseTo(1);expect(beta([2,4,6],[1,2,3])).toBeCloseTo(2);expect(drawdown([100,120,90,110])).toEqual({current:110/120-1,max:-.25});expect(zScore(3,[1,2,3])).toBeCloseTo(1)});it('backtests without exposing future bars and charges costs',()=>{const seen:number[]=[];const bars=Array.from({length:5},(_,i)=>({timestamp:String(i),prices:{a:100+i*10}}));const result=runBacktest(bars,{name:'audit',onBar:c=>{seen.push(c.history.length);return[{assetId:'a',targetWeight:1}]}},{initialCash:1000,feeRate:.01,rebalanceEvery:1});expect(seen).toEqual([1,2,3,4,5]);expect(result.trades).toBe(5);expect(result.equityCurve[0]!.value).toBeLessThan(1000);expect(result.totalReturn).toBeGreaterThan(0)})});
