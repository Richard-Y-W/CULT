# Contributing

Run `npm run setup`, then `npm test`, `npm run lint`, and `npm run typecheck` before opening a change. Domain calculations belong in packages, not UI components. Account changes require balanced, append-only ledger entries. Synthetic generation must remain deterministic.
